# Node
node_modules/
scraper-node/dist/
scraper-node/sea-config.json

# Rust / Tauri build output
src-tauri/target/
src-tauri/gen/

# Sidecar binaries (built by `npm run build:sidecar` and `node setup-chromium.mjs`)
src-tauri/binaries/

# Generated icon source (kept for reproducibility but not needed in repo)
src-tauri/icons/_source.png

# Mobile icons (desktop-only app — android/ios generated by `tauri icon` but not needed)
src-tauri/icons/android/
src-tauri/icons/ios/
